# ============================================================
# CausasPro Bot PJUD - Ejecución automática
# Se ejecuta 2 veces al día (10AM y 4PM hora Chile)
# ============================================================

name: 🤖 Bot PJUD - Scraping automático

on:
  # MODO PRIMERA CARGA: 6 ejecuciones diarias para actualizar 900 causas rápido
  schedule:
    - cron: '0 12 * * 1-5'   # 8:00 AM Chile
    - cron: '0 14 * * 1-5'   # 10:00 AM Chile
    - cron: '30 16 * * 1-5'  # 12:30 PM Chile
    - cron: '0 19 * * 1-5'   # 3:00 PM Chile
    - cron: '0 21 * * 1-5'   # 5:00 PM Chile
    - cron: '0 6 * * 1-5'    # 2:00 AM Chile
  
  # También se puede ejecutar manualmente
  workflow_dispatch:
    inputs:
      max_causas:
        description: 'Máximo de causas a procesar'
        required: false
        default: '25'
      mode:
        description: 'Modo de ejecución'
        required: false
        default: 'normal'
        type: choice
        options:
          - normal
          - urgent_only
          - test_single

jobs:
  scrape:
    runs-on: ubuntu-latest
    timeout-minutes: 60  # Max 1 hora por ejecución
    
    steps:
      - name: 📥 Checkout
        uses: actions/checkout@v4
      
      - name: 🟢 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
      
      - name: 📦 Install dependencies
        run: npm install
      
      - name: 🎭 Install Playwright
        run: npx playwright install chromium --with-deps
      
      - name: 🤖 Run Bot
        env:
          PJUD_RUT: ${{ secrets.PJUD_RUT }}
          PJUD_PASSWORD: ${{ secrets.PJUD_PASSWORD }}
          NEXT_PUBLIC_SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
          BOT_MAX_CAUSAS: ${{ github.event.inputs.max_causas || '25' }}
          BOT_MODE: ${{ github.event.inputs.mode || 'normal' }}
          SKIP_HOUR_CHECK: '1'
        run: npx tsx src/bot/index.ts
      
      - name: 📸 Upload screenshots on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: bot-error-screenshots
          path: /tmp/bot_error_*.png
          retention-days: 7
